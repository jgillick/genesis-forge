# Deployment

Exporting a trained environment for a real robot, and the simulation-free runtime
that replays it. See the [deployment guide](../../guide/deployment.md) for the
end-to-end walkthrough.

The training-side API lives in `genesis_forge.deployment`; the robot-side runtime is
the separate `genesis-forge-runtime` package.
