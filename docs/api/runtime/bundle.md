# Bundle

What `genesis_forge.deployment.export` writes and the robot reads: the manifest,
the recorded golden samples, and the policy files. A bundle is either a directory or
a single `.gfb` archive, and these read either.

::: genesis_forge_runtime.load_bundle

::: genesis_forge_runtime.Bundle

::: genesis_forge_runtime.load_manifest

::: genesis_forge_runtime.save_bundle
