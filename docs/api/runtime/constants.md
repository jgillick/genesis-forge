# Constants

The bundle format's vocabulary: filenames, the schema version the runtime reads,
and the names it records for policy formats. These are the manifest's contract, so
they are deliberately stable — tooling that never imports the rest of this package
can still rely on them.

::: genesis_forge_runtime.constants
