# Errors

Every failure the runtime raises names what went wrong and, where it can, what to
do about it — a missing observation says which entry, a rejected bundle says which
field.

::: genesis_forge_runtime.BundleError

::: genesis_forge_runtime.MalformedBundleError

::: genesis_forge_runtime.SchemaVersionError

::: genesis_forge_runtime.ObservationError

::: genesis_forge_runtime.ActionError
