# Manifest

The deployment contract, read from the bundle's `manifest.json`. Every field is
plain data, so a bundle can be inspected without running anything.

::: genesis_forge_runtime.Manifest

::: genesis_forge_runtime.ObservationLayout

::: genesis_forge_runtime.ObservationEntry

::: genesis_forge_runtime.ActionManagerSpec

::: genesis_forge_runtime.ActuatorSpec

::: genesis_forge_runtime.Provenance
