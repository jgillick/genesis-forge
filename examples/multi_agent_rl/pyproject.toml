[project]
name = "go2-masq"
version = "0.1.0"
description = "Go2 locomotion with SKRL MAPPO (MASQ-style per-leg agents) on Genesis Forge."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "genesis-forge",
    "genesis-world>=0.4.0",
    "tensorboard",
]

[tool.uv.sources]
genesis-forge = { path = "../../" }
