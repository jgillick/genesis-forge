[project]
name = "genesis-forge-multi-agent-rl-example"
version = "0.1.0"
description = "Genesis Forge example demonstrating multi-agent reinforcement learning."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "genesis-forge",
    "genesis-world>=0.4.0",
    "tensorboard",
]

[tool.uv.sources]
genesis-forge = { path = "../../packages/genesis-forge" }
