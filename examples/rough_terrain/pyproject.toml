[project]
name = "genesis-forge-rough-terrain-example"
version = "0.1.0"
description = "Go2 rough terrain locomotion training with Genesis Forge."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "genesis-forge",
    "rsl-rl-lib~=5.0",
    "tensorboard",
]

[tool.uv.sources]
genesis-forge = { path = "../../" }
