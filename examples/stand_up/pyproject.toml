[project]
name = "genesis-forge-stand-up"
version = "0.1.0"
description = "Go2 stand-up training example for Genesis Forge."
requires-python = ">=3.10"
dependencies = [
    "genesis-forge",
    "rsl-rl-lib~=5.0",
    "tensorboard",
]

[tool.uv.sources]
genesis-forge = { path = "../../packages/genesis-forge" }
